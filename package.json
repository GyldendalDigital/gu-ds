{
  "name": "gu-ds",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "clean": "turbo clean && rm -rf .turbo node_modules",
    "lint": "turbo lint",
    "dev": "turbo dev",
    "build": "turbo build",
    "tokens": "turbo tokens",
    "storybook": "turbo storybook",
    "css": "turbo css"
  },
  "dependencies": {
    "@changesets/cli": "latest",
    "prettier": "latest",
    "turbo": "latest"
  }
}
